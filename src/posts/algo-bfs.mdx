---
title: BFS
publishedAt: 2025-07-30
categories: ETC,Algorithm
---

# BFS

- 큐

```
queue<int> q;

check[1] = true;

q.push(1);

while(!q.empty()) {
    int x = q.front();
    q.pop();
    printf("%d", x);
    for(int i=0; i<=n; i++) {
        if(a[x][i] == 1 && check[i] == false) {
            check[i] = true;
            q.push(i);
        }
    }
}
```
